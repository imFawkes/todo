<div class= "task-new">
  <%= form_with model: [@user, Task.new] do |form| %>
    <span>Add a task:</span>
    <span><%= form.text_field :content %></span>
    <span><%= form.submit "Add Task" %></span>
  <% end %>
</div>