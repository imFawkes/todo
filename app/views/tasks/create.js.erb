element = document.querySelector(".listAll")
element.outerHTML = "<%= j render partial: "tasks/index", locals: { user: user } %>"

// clear task input
input = document.getElementById("task_content")
input.value = ""


// insert created task as 1st element in unsorted list
// parentElement = document.querySelector(".listAll")
// theFirstChild = parentElement.firstChild;
// newElement = document.createElement("p");
// parentElement.insertBefore(newElement, theFirstChild);
// newElement.outerHTML = "<%= j render partial: "tasks/show", locals: { user: user, task: task } %>";

